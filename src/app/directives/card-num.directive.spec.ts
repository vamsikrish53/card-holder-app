import { CardNumDirective } from './card-num.directive';

describe('CardNumDirective', () => {
  it('should create an instance', () => {
    const directive = new CardNumDirective();
    expect(directive).toBeTruthy();
  });
});
